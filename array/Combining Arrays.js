/*
5. Combining Arrays
Instructions:

Create two arrays of your choice.
Use the concat method to combine the two arrays into a new array.
Print both the original arrays and the combined array using console.log().
*/
let movie = ['3idiot', 'shadi main jorur aana', 'ashique'];
let avengers = ['thor', 'iron man', 'spiderman'];

// Combine using concat
let newArray = movie.concat(avengers);

// Print all arrays
console.log("Movie Array:", movie);
console.log("Avengers Array:", avengers);
console.log("Combined Array:", newArray);
